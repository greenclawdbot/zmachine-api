import type { DataSaver, DataLoader, StatusSetter, ScreenSetter, NoiseMaker, TranscriptWriter, CommandRecorder, CommandReplayer, KeyPress } from './types';
export type { Screen, Status, Window, Line, Char, LetterKind, IconKind, Letter, Icon, ScreenSetter, DataType, DataSaver, DataLoader, NoiseType, NoiseMaker, TranscriptWriter, CommandRecorder, CommandReplayer, GameStatusType, GameStatus, StatusSetter, LengthOneString, SpecialCharacter, KeyPress, } from './types';
export default class ZMachine {
    static attachScreenSetter(setter: ScreenSetter): void;
    static attachStatusSetter(setter: StatusSetter): void;
    static loadStory(file: number[]): void;
    static keyPress(key: KeyPress): void;
    static setAppSize(width: number, height: number): void;
    static attachDataSaver(saver: DataSaver): void;
    static attachDataLoader(loader: DataLoader): void;
    static attachNoiseMaker(noiseMaker: NoiseMaker): void;
    static attachTranscriptWriter(transcriptWriter: TranscriptWriter): void;
    static attachCommandRecorder(commandRecorder: CommandRecorder): void;
    static attachCommandReplayer(commandReplayer: CommandReplayer): void;
}
