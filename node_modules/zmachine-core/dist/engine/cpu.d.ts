import { Arg } from '../ops/types';
declare type LogOpOptions = {
    cycles?: number;
    opcode?: number;
    pc?: number;
    name?: string;
    args?: Arg[];
};
declare class CPU {
    running: boolean;
    debug: boolean;
    logOp(options?: LogOpOptions): void;
    run(): Promise<void>;
}
declare const instance: CPU;
export default instance;
