import { Arg } from './types';
export default class GameOps {
    static save(): Promise<void>;
    static save_v5(table: Arg, bytes: Arg, name: Arg, prompt: Arg): Promise<void>;
    static restore(): Promise<void>;
    static restore_v5(table: Arg, bytes: Arg, name: Arg, prompt: Arg): Promise<void>;
    static save_undo(): number;
    static restore_undo(): number;
    static restart(): void;
    static quit(): void;
    static verify(): number;
    static piracy(): number;
}
