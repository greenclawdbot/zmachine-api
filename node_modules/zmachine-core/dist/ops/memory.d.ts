import { Arg } from './types';
export default class MemoryOps {
    static copy_table(first: Arg, second: Arg, size: Arg): void;
    static loadb(array: Arg, byteIndex: Arg): number;
    static loadw(array: Arg, wordIndex: Arg): number;
    static scan_table(x: Arg, table: Arg, len: Arg, form: Arg): number;
    static storeb(array: Arg, byteIndex: Arg, value: Arg): void;
    static storew(array: Arg, wordIndex: Arg, value: Arg): void;
}
