declare type Token = {
    zscii: number[];
    start: number;
};
export default class Dictionary {
    static tokenise(text: number, parse: number, dictionary: number | null, skipUnknown: boolean): void;
    static parse(tokens: Token[], parse: number, dictionary: number | null, skipUnknown: boolean): void;
    static encodeText(zsciiText: number, length: number, from: number, codedText: number): void;
    static dump(): number[][];
}
export {};
