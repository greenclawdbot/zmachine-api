export default class Game {
    static loadStory(file: number[]): void;
    static startStory(): void;
    static initHeader(): void;
    static initPC(): void;
    static saveGame(): Promise<boolean>;
    static restartGame(): void;
    static restoreGame(): Promise<boolean>;
    static saveTable(table: number, bytes: number, name: number, doPrompt: boolean, doNotPrompt: boolean): Promise<boolean>;
    static restoreTable(table: number, bytes: number, name: number, doPrompt: boolean, doNotPrompt: boolean): Promise<number>;
    static auxFileName(nameBytes: number[]): string;
    static saveUndo(): boolean;
    static restoreUndo(): boolean;
    static verifyChecksum(): boolean;
}
