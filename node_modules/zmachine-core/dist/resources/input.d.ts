import { Timer } from '../utils/types';
import { KeyPress } from '../api/types';
export default class Input {
    static untimedRead(text: number, parse: number): Promise<void>;
    static timedRead(text: number, parse: number, time: number, routine: number, resuming?: boolean, casedInput?: number[], resumeValue?: number): Promise<void>;
    static finishRead(text: number, parse: number, terminator: number): void;
    static getTerminatingCharacters(): number[];
    static read(text: number, timer?: Timer | null, casedInput?: number[]): Promise<number>;
    static readInput(inputBuffer: number[], // modified in place
    maxInputLength: number, timer: Timer | null): Promise<number>;
    static timedReadChar(time: number, routine: number, resuming?: boolean, resumeValue?: number): Promise<void>;
    static untimedReadChar(): Promise<void>;
    static readChar(timer: Timer | null): Promise<number>;
    static replayCommand(): Promise<[number[], number]>;
    static unescapeCommand(escaped: string): [number[], number];
    static keyPress(keyPress: KeyPress): boolean;
}
