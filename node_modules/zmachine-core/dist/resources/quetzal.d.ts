import { Frame, SavedGameState } from '../utils/types';
export default class Quetzal {
    static save(state: SavedGameState, storyFile: number[]): number[];
    static restore(saveFile: number[], storyFile: number[]): SavedGameState;
    static chunk(id: string, data: number[]): number[];
    static unchunk(file: number[]): {
        id: string;
        data: number[];
        remainder: number[];
    };
    static chunkCMem(dynamicMemory: number[], storyFile: number[]): number[];
    static unpackCMem(data: number[], storyFile: number[]): number[];
    static chunkStks(frames: Frame[]): number[];
    static unpackStks(data: number[]): Frame[];
    static chunkIFhd(file: number[], pc: number): number[];
    static unpackIFhd(data: number[], file: number[]): number;
    static idToBytes(id: string): number[];
    static int32ToBytes(num: number): number[];
    static bytesToInt32(bytes: number[]): number;
    static int24ToBytes(num: number): number[];
    static bytesToInt24(bytes: number[]): number;
    static int16ToBytes(num: number): number[];
    static bytesToInt16(bytes: number[]): number;
}
