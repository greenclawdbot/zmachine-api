import { Arg } from '../ops/types';
import { InterruptCallback } from '../utils/types';
export default class Routine {
    static branch(value: number): Promise<void>;
    static call(routine: number, store: boolean, arg1: Arg, arg2: Arg, arg3: Arg, arg4: Arg, arg5: Arg, arg6: Arg, arg7: Arg, interruptCallback: InterruptCallback | null): void;
    static jump(offset: number): void;
    static return(value: number): Promise<void>;
    static store(value: number): void;
    static CheckArgumentNumber(argumentNumber: number): boolean;
    static catch(): void;
    static throw(value: number, frameCount: number): Promise<void>;
}
