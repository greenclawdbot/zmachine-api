import { KeyPress } from '../api/types';
export default class ZSCII {
    static outputZSCIIToString(zscii: number[]): string;
    static getExtraCharacter(code: number): number;
    static lookupExtraCharacter(charCode: number): number;
    static stringToInputZSCII(keyPress: KeyPress): number;
}
