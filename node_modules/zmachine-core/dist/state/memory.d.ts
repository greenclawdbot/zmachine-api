declare class Memory {
    memory: number[];
    length: number;
    static: number;
    setMemory(file: number[]): void;
    getByte(byteAddr: number): number;
    setByte(byteAddr: number, byte: number): void;
    getWord(byteAddr: number): number;
    setWord(byteAddr: number, word: number): void;
    getBytes(byteAddr: number, len: number): number[];
    setBytes(byteAddr: number, bytes: number[]): void;
    getDynamicMemory(): number[];
    setDynamicMemory(memory: number[]): void;
}
declare const singleton: Memory;
export default singleton;
