declare type Mode = 'RANDOM' | 'SEQUENTIAL' | 'SEEDED';
declare class Random {
    mode: Mode;
    last: number;
    max: number;
    init(): void;
    random(range: number): number;
    seed(value: number): void;
}
declare const singleton: Random;
export default singleton;
