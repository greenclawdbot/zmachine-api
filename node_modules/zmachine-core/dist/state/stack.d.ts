import { Frame } from '../utils/types';
declare class Stack {
    frames: Frame[];
    init(): void;
    pushFrame(frame: Frame): void;
    popFrame(): Frame;
    frameCount(): number;
    currentFrame(): Frame;
    pushStack(word: number): void;
    popStack(): number;
    getLocal(localNum: number): number;
    setLocal(localNum: number, word: number): void;
    saveFrames(): Frame[];
    restoreFrames(frames: Frame[]): void;
}
declare const singleton: Stack;
export default singleton;
