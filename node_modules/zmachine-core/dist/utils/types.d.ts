export interface InterruptCallback {
    (value: number): Promise<void>;
}
export interface Frame {
    returnPC: number;
    store: boolean;
    storeVar: number;
    locals: number[];
    stack: number[];
    argFlags: number;
    interruptCallback: InterruptCallback | null;
}
export interface SavedGameState {
    dynamicMemory: number[];
    pc: number;
    frames: Frame[];
}
export declare type Timer = Promise<0>;
